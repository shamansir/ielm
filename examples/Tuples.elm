import Array

( 1, 2 )

( "a", [ 2, 6, 5 ])

( "a", Array.fromList [ 2, 6, 5 ])

type alias Test2 = ( String, String )

c : Test2
c = ( "a", "b" )

( "a", "b", "c" )

[ (1, 'a'), (2, 'b') ]

[ (1, 'a'), (2, 'b'), (3, 'c') ]

c
