type alias Test =
    { a: Int
    , b: String
    }

foo : Test
foo = { a = 1, b = "f" }

foo

Just foo

Nothing

Ok foo

Err foo

Err "Error with Message"

type TestApp e x = TestApp e x

a : TestApp String Int
a = TestApp "f" 2

a
